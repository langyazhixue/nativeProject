import {StyleSheet} from 'react-native';
import {variableStyle} from './variables';
export const layoutStyles = StyleSheet.create({
  appContainer: {
    flex: 1,
    backgroundColor: variableStyle.bgColor,
  },
});
